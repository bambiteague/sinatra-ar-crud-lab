// view should use ERB to iterate over @articles and render them on the page
<ul>
<% @articles.each do |article| %>
  <li>
    <a href="/articles/<%= article.id %>">
      <%= article.title %> 
    </a>
  </li>
<% end %>
</ul>